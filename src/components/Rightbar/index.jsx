import React from "react";
import { Box } from "@mui/material";

const Rightbar = () => {
  return (
    <Box sx={{ backgroundColor: "pink" }} flex={2}>
      Rightbar
    </Box>
  );
};

export default Rightbar;
